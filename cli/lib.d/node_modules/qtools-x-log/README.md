qtools-x-log

Logging done the way I want it.

		annotation,
		error,
		result,
		status,
		debug: debugFunction,
		emphatic,
		verbose: verboseFunction,
		setProcessFilesDirectory,
		getProcessFilesDirectory,
		saveProcessFile,
		progress: progressFunction,
		setProgressMessageStatus,